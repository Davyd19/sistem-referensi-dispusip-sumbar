<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Sistem Informasi Unit Layanan Dispusip Sumbar</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body class="bg-slate-50 min-h-screen flex flex-col font-sans">

<nav class="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm w-full">
    <div class="w-full px-4 sm:px-6 h-16 flex justify-between items-center gap-4">
        
        <% 
           let homeLink = '/';
           // Cek variable query dari controller
           if (typeof query !== 'undefined' && query.id_ruangan) {
               homeLink = `/books?id_ruangan=${query.id_ruangan}`;
           }
        %>

        <div class="flex items-center cursor-pointer group flex-shrink-0" onclick="window.location='<%= homeLink %>'">
            <img src="/image/assets/smarLIB.png" class="h-10 sm:h-12 mr-2 sm:mr-3 group-hover:opacity-90 transition">
            <div class="flex flex-col">
                <h1 class="text-sm sm:text-lg font-bold text-gray-900 group-hover:text-blue-700 transition leading-tight">Sistem Informasi Unit Layanan</h1>
                <p class="text-[10px] sm:text-xs text-gray-500">Dispusip Provinsi Sumatera Barat</p>
            </div>
        </div>

        <% if (typeof currentRuangan !== 'undefined' && currentRuangan) { %>
            <div class="flex items-center justify-end">
                <div class="hidden md:flex items-center bg-blue-50 px-4 py-1.5 rounded-lg border border-blue-100">
                    <div class="w-8 h-8 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 mr-3">
                        <i class="fas fa-map-location-dot"></i>
                    </div>
                    <div class="flex flex-col items-end">
                        <span class="text-[10px] text-gray-500 uppercase tracking-wider font-bold leading-none mb-0.5">Lokasi Katalog</span>
                        <span class="text-sm font-bold text-blue-800 leading-none truncate max-w-[200px]"><%= currentRuangan.nama_ruangan %></span>
                    </div>
                </div>

                <div class="md:hidden flex flex-col items-end">
                    <span class="text-[9px] text-gray-400 font-medium">Lokasi:</span>
                    <span class="text-xs font-bold text-blue-700 bg-blue-50 px-2 py-0.5 rounded border border-blue-100 truncate max-w-[120px]">
                        <%= currentRuangan.nama_ruangan %>
                    </span>
                </div>
            </div>
        <% } %>

    </div>
</nav>

<main class="flex-grow w-full px-4 sm:px-6 py-4">