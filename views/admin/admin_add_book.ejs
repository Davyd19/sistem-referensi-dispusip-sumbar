<%- include('partials/header', { title: 'Tambah Buku', active: 'add' }) %>

<link href="https://cdn.jsdelivr.net/npm/tom-select/dist/css/tom-select.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/tom-select/dist/js/tom-select.complete.min.js"></script>

<h1 class="text-2xl font-bold mb-6">➕ Tambah Buku Baru</h1>

<form 
    action="/admin/books/add" 
    method="POST" 
    enctype="multipart/form-data"
    class="bg-white p-6 rounded-lg shadow space-y-6"
>

    <!-- INFORMASI DASAR -->
    <div class="grid grid-cols-2 gap-6">
        <div class="space-y-4">
            <div>
                <label class="font-medium">Judul</label>
                <input type="text" name="title" required class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Judul Asli</label>
                <input type="text" name="original_title" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Pernyataan Tanggung Jawab</label>
                <input type="text" name="statement_of_responsibility" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Series Title</label>
                <input type="text" name="series_title" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Edisi</label>
                <input type="text" name="edition" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Tahun Terbit</label>
                <input type="text" name="publish_year" maxlength="4" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Tempat Terbit</label>
                <input type="text" name="publish_place" class="w-full p-2 border rounded">
            </div>
        </div>

        <div class="space-y-4">
            <div>
                <label class="font-medium">Deskripsi Fisik</label>
                <input type="text" name="physical_description" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Content Type</label>
                <input type="text" name="content_type" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Media Type</label>
                <input type="text" name="media_type" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Carrier Type</label>
                <input type="text" name="carrier_type" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">ISBN</label>
                <input type="text" name="isbn" class="w-full p-2 border rounded">
            </div>
            <div>
                <label class="font-medium">Call Number</label>
                <input type="text" name="call_number" class="w-full p-2 border rounded">
            </div>
        </div>
    </div>

    <!-- ABSTRAK DAN CATATAN -->
    <div>
        <label class="font-medium">Abstrak</label>
        <textarea name="abstract" rows="4" class="w-full p-2 border rounded"></textarea>
    </div>

    <div>
        <label class="font-medium">Catatan / Notes</label>
        <textarea name="notes" rows="3" class="w-full p-2 border rounded"></textarea>
    </div>

    <!-- AUTHOR & PUBLISHER -->
    <div class="grid grid-cols-2 gap-6">
        <div>
            <label class="font-medium">Penulis (Authors)</label>
            <select name="authors[]" id="author-select" class="w-full p-2 border rounded" multiple>
                <% authors.forEach(a => { %>
                    <option value="<%= a.id %>"><%= a.name %></option>
                <% }) %>
            </select>
        </div>

        <div>
            <label class="font-medium">Penerbit (Publisher)</label>
            <select name="publisher_id" id="publisher-select" class="w-full p-2 border rounded">
                <option value="">-- Pilih Penerbit --</option>
                <% publishers.forEach(p => { %>
                    <option value="<%= p.id %>"><%= p.name %></option>
                <% }) %>
            </select>
        </div>
    </div>

    <!-- CATEGORY & SUBJECT -->
    <div class="grid grid-cols-2 gap-6">
        <div>
            <label class="font-medium">Kategori</label>
            <select name="category_id" id="category-select" class="w-full p-2 border rounded">
                <option value="">-- Pilih Kategori --</option>
                <% categories.forEach(c => { %>
                    <option value="<%= c.id %>"><%= c.name %></option>
                <% }) %>
            </select>
        </div>

        <div>
            <label class="font-medium">Subjek (Subjects)</label>
            <select name="subjects[]" id="subject-select" class="w-full p-2 border rounded" multiple>
                <% subjects.forEach(s => { %>
                    <option value="<%= s.id %>"><%= s.name %></option>
                <% }) %>
            </select>
        </div>
    </div>

    <!-- INFORMASI LAIN -->
    <div class="grid grid-cols-2 gap-6">
        <div>
            <label class="font-medium">Bahasa</label>
            <input type="text" name="language" class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="font-medium">Work Type</label>
            <input type="text" name="work_type" class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="font-medium">Target Audience</label>
            <input type="text" name="target_audience" class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="font-medium">Lokasi Rak (Shelf Location)</label>
            <input type="text" name="shelf_location" placeholder="Contoh: Rak A1" class="w-full p-2 border rounded">
        </div>
    </div>

    <!-- STOK DAN GAMBAR -->
    <div class="grid grid-cols-2 gap-6">
        <div>
            <label class="font-medium">Total Stok</label>
            <input type="number" name="stock_total" class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="font-medium">Stok Tersedia</label>
            <input type="number" name="stock_available" class="w-full p-2 border rounded">
        </div>
    </div>

    <div>
        <label class="font-medium">Upload Cover</label>
        <input type="file" name="image" accept="image/*" class="w-full p-2 border rounded">
    </div>

    <button 
        type="submit"
        class="bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700"
    >
        Simpan Buku
    </button>

</form>

<script>
    function initTomSelect(selector, placeholderText, isMultiple = true) {
        return new TomSelect(selector, {
            plugins: ['remove_button'],
            create: true,
            sortField: { field: "text", direction: "asc" },
            placeholder: placeholderText,
            allowEmptyOption: !isMultiple,
            render: {
                item: function(data, escape) {
                    return `<div class="bg-blue-100 text-blue-800 rounded px-2 py-1 mr-1 mb-1 inline-block">${escape(data.text)}</div>`;
                },
                option: function(data, escape) {
                    return `<div class="hover:bg-blue-100 hover:text-blue-800 p-1">${escape(data.text)}</div>`;
                }
            }
        });
    }

    // Inisialisasi TomSelect
    initTomSelect("#category-select", "Cari atau tambah kategori...");
    initTomSelect("#author-select", "Cari atau tambah penulis...");
    initTomSelect("#subject-select", "Cari atau tambah subjek...");
    initTomSelect("#publisher-select", "Pilih atau tambah penerbit..."); // single select
</script>

<%- include('partials/footer') %>